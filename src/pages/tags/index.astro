---
import Layout from '../../layouts/Layout.astro'
import tagsJson from '../../data/tags.json'
import tagpagesJson from '../../data/tagpages.json'
---

<Layout title="Tags">
	<div class="content">
	<h1>Tags</h1>
	<ul>
		{Object.entries(tagpagesJson).map(([i, className]) =>
			<li><a href={'/tags/' + i}>{i}</a></li>)}
	</ul>
	</div>
</Layout>